# Mac 手动锁屏连续运行验收

2026-09-08，探针源码 `79cfeb8`。用户手动锁屏、解锁并确认“已解锁”；没有再重启电脑。实际结果：`CLAWBOT_SCREEN_LOCK_CONTINUITY_VERIFIED`。

## 实测结果

最终有效锁屏段为 08:20:40.232–08:22:11.103 UTC，连续 **90.871 秒**；08:22:16.848 UTC 采样确认解锁并完成数据核验。此前一次短暂 locked 样本不满足持续时间，未计入通过时长。全程共 233 个样本，每次要求有序且相邻不超过 20 秒；没有以等待时间或容器 uptime 补算锁屏。

- 使用 macOS `IOConsoleLocked` 的明确布尔状态，满足 unlocked → 持续 locked → unlocked。
- 同一启动会话、同一精确 LaunchAgent/PID、两容器 ID/启动时间/重启次数保持不变。
- 每次宿主状态与状态页都新鲜健康，AC 电源为真。
- 同一 OpenClaw 容器内对 `https://chatgpt.com/` 的无凭据 HTTPS HEAD 持续取得响应。
- 前后均使用原 `p1-state-snapshot.json` 校验账本及回执两库全表，且原基线文件哈希不变；没有保存新基线来替代原记录。

宿主仍为 `94785405782939c1f32c1f647f8839450ee2195d`，镜像仍为 `sha256:1d3fa022e1259ecee4a5bde1a5763ac5b7b32b7dc85373154b704ea0d10beebd`。完整只含运行证据的私有回执位于 `~/Library/Application Support/Clawbot/operations/screen-checks/1788854405067-02c5b8e1-a696-43f1-8b21-773ad130cc91.json`，不提交 Git。

## 验证边界

本项证明开盖、插电的隔离服务在实际锁屏期间持续运行，解锁后持久业务数据一致。无凭据 HTTP 响应只证明传输可达，不代表模型授权、模型调用、微信轮询或 Tunnel 可用。没有执行真实微信收发、生产迁移、Windows 停服、独立息屏或合盖测试，亦不能替代 24–48 小时连续观察。

准备时只读探针因不同文件所有者而无法读取已有基线；修正为仅在三个只读数据挂载上添加 read/search 权限，不改原文件权限，不授予写权限。早期失败记录为 no-acceptance；最终通过来自本次完整观察。探针不负责锁屏或解锁，不停止或重建服务，等待用户期间不占用运维锁。
