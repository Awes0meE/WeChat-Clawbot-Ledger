# Mac P1 验收与 P2 接续

日期：2026-09-08。分支：`feat/mac-docker-runtime`。

**P1 隔离原型已通过，正在推进 P2–P6 的交接前工作。Windows 仍是生产微信接收端。** 用户已选择保留 OpenClaw 2026.8.2，采用受限 MCP 适配，并授权继续开发至完整迁移。尚未复制生产身份、迁移正式数据、启动 Mac 生产接收器或切换 Tunnel。最新状态以 [完整清单](../mac-before-windows-checklist.md) 为入口；下方保留各次验收的具体范围。

## 当前运行组合

- Mac M1，8 GiB 内存。用户已接电，家中 Wi-Fi，计划开盖亮屏长期运行。电源只读核验为插电 `sleep=0`、`displaysleep=0`、`tcpkeepalive=1`。
- Docker Desktop 4.90.0、Engine 29.7.2、Compose v5.5.1；实际分配 2 CPU、约 4 GiB 内存，登录后启动，关闭资源节省暂停。登录前无人值守启动尚未实现。
- ARM64 固定镜像 OpenClaw 2026.8.2、Node 24.19.0、SQLite 3.53.3、官方 Codex harness 2026.8.2、Codex CLI 0.151.0。基础镜像 digest 见 Dockerfile/Compose，MCP SDK 为基础镜像中的 1.30.0。
- 派生镜像 `clawbot-openclaw-test:p1`，项目 `clawbot-test`。账本仅在共享命名空间内监听 `127.0.0.1:18888`，Gateway 仅回环 `18789`；没有 Docker 宿主端口发布。
- 微信通道关闭、无真实账号。测试 profile 核验 Linux、只读镜像标记、固定状态/秘密路径、唯一合成 owner。生产默认继续固定 `8888`。
- `verify-test-host.mjs` 已通过 ARM64、当前镜像、项目命名卷、非 root、只读根目录、cap-drop、no-new-privileges、共享 namespace 及无宿主端口检查。
- session-memory Linux 包装器核验固定上游版本及 SHA-256，实际 managed hook 已加载。

## 六工具与真实模型验收

用户已完成 ChatGPT 登录，无需再次授权。当前实际调用为 `openai/gpt-5.6-sol`、`agentHarnessId=codex`、thinking=low。

Mac 镜像通过 `bookkeeping-linux.ts` 注册同名受限历史工具，使用官方 MCP SDK 查询独立令牌保护的本地 MCP；不再让 harness 原生装载 MCP server。`tools.profile=full` 与精确六工具 allowlist 保留。历史查询只授予配置中的微信 owner，固定支出账户与 SGD、每页最多十笔，验证分页和时间，限制响应大小与超时，禁止重定向，过滤非业务字段。

先前尝试的 HTTP MCP `cwd` 补丁已删除。验收检查官方 `codex-mcp-config-lF3PPwrW.js` 仍为原 SHA-256；没有修改 harness 的严格工具限制。

两层实测均已通过：

1. `verify-owner-model.mjs` 读取实际运行报告，确认恰好六项业务工具，固定模型/harness，以及原样的上游 MCP 模块。它不依赖模型自己列举工具。
2. `verify-channel-model.mjs --suite` 通过官方完整消息派发和微信插件发送前钩子，验证明确记账、提出确认、确认入账、再次提出确认、取消、汇总、精确金额搜索、历史明细。八个合成回合全部通过，运行时持久工具事件证实六项工具均成功执行。每回合最终回复与该消息的持久权威回复一致；历史准确输出三笔。没有调用真实微信发送接口。

实测还修复了三个问题：

- Mac 初始化漏配 `allowConversationAccess`，官方运行时会过滤消息关联、时间上下文及结束清理钩子。现显式授权账本插件所需的对话/提示上下文能力，启动策略同时校验六工具、固定模型及这两项能力。
- 原代理说明超过 4000 字符限额。Mac 单文件/总注入限额调整为 8000/16000，并在启动前检查截断。
- 执行实例的持久桥接曾另建回复身份，与原 after-tool 钩子产生两份回复记录，导致唯一回复恢复失败。现沿用原授权 run 的身份。新增回归在旧代码上复现失败，修复后通过；没有双写账本。此源码修复尚未部署 Windows。

## 其他验证及范围

- P1 检查点 Mac 可移植回归为 506 通过、0 失败、1 跳过（507 项）；前次新增运维测试后为 522 通过、0 失败、1 跳过（523 项）；最新 e93ffb4 检查点为 539 通过、0 失败、1 跳过（540 项）。均明确排除 11 个 Windows 专用文件。微信稳定 ID 插件此前构建和 16 项测试通过。Windows PowerShell 回归仍待 Windows 执行，不能称全平台全量通过。
- 真实 HTTP/MCP、固定 0.01 SGD 消息去重及 SQLite 重新打开、跨实例小额写入/确认和陌生人拒绝通过。无备注 MCP 记录按缺省“无”显示；显式非法备注仍拒绝。
- P2 当前镜像已再次验证 origin 停止/启动、origin/Gateway 删除重建、Docker VM 重启后显式恢复：账本与回执库全部表结构、计数、内容哈希一致，固定消息不重复写入，模型授权仍可用。这是显式恢复，不代表自动守护或整机登录前恢复。
- 独立 Docker bridge 命名空间访问账本被拒绝，共享 loopback 内可用。

## P2 已完成与待做

- 本机只读状态页已启动：[http://127.0.0.1:18990](http://127.0.0.1:18990)。显示测试容器健康、CPU/内存、电源、立即空闲磁盘和迁移阶段；只接受本机 Host/Origin 与 GET/HEAD，HTTP 边界和浏览器检查通过。它是概览，不代替守护身份验收；现已安装登录启动服务 `com.clawbot.mac-test-host`，从仓库外只读副本运行；实际整机重启后人工登录、服务自动恢复及两库一致检查已通过，见 [重启验收](2026-09-08-mac-reboot-check.md)。
- `backup-test-state.mjs` 已完成一次七卷离线一致快照、AES-256-GCM 加密、解密认证、恢复到新建且无网络的验证卷、全文件哈希/权限校验、两库完整性/全表内容核对。验证卷检查后删除，源卷未覆盖，服务自动恢复。备份和独立密钥位于 Git 之外的本机 `Library/Application Support/Clawbot/`，不进入模型上下文。加密测试覆盖错误密钥、篡改密文与篡改发布元数据。
- Linux origin 守护已实现并通过隔离故障矩阵：固定程序路径/二进制哈希、显式启动参数、配置哈希/策略、回环端口及 PID 1 socket owner、健康 JSON 和页面指纹。健康丢失立即关闭本进程子发布器，恢复需连续两次验证；错误配置与返回健康页面的冒牌 origin 均拒绝。守护被杀与 origin 退出均无遗留发布器。另有同容器保活下只杀父进程的验证，证明 Linux parent-death 机制，不依赖容器被清理造成的假阳性。
- 实际 cloudflared 2026.8.3 ARM64 二进制使用固定 SHA-256；真实二进制离线配置校验通过。完整生命周期故障测试使用本地替身发布器，无生产凭据或公网连接。正式路径只接受 Ledger 唯一 hostname 与 404 兜底，还要求 Windows 三项停机回执及对应最终快照/源码身份。回执是操作人员核验后的声明，不能当成远程持续在线检查。
- 守护共享账本 PID namespace，运行于相同 UID，因而是受信组件，能通过 proc 视图访问 origin 文件；不能称内核隔离的只读账本沙箱。Docker 宿主身份、镜像、挂载与命名空间仍需主机发布检查；模型没有 Docker socket。
- 备份加入跨操作互斥、至少 10 GiB 主机余量、归档和离线恢复双份容量估算、20 GiB 备份总预算；预算满则报错，不自动删除已有备份。新版本再次完成七卷备份、恢复及服务恢复。残留锁不自动抢占，需要先核验是否还有 Docker helper。
- 生产 Docker build target 和独立 Compose 已准备；生产路径固定 `/var/lib/clawbot`，外部卷必须事先导入。OpenClaw 生产配置只读且核验哈希、实际六工具/单一 owner/单一路由，接收器定期检查切换门禁。生产构建和只读发布入口已完成实际构建；缺少 activation 时真实生产镜像在无网络、无卷环境立即拒绝启动。后续加固还要求镜像内只读源码标记与回执一致，防止旧镜像借用新发布回执。没有生产数据时不得用测试卷顶替。
- 主机登录后状态与受控恢复入口已写好，发布须干净提交、镜像内容哈希、仓库外只读副本及逐文件校验。自动恢复只接管已识别容器；未知/缺失容器、替换的 namespace 和残留操作锁均留待核验，不自动收编。状态页会显示最近已验证备份。已安装并完成自动恢复实测：受控停止两个测试服务及保存一致基线后，只由后台服务启动，约 22 秒恢复健康，两库数据核对不变。
- 尚待：真实主机磁盘错误故障注入、生产主机运行器及导入流程按 Windows 实际布局接入、异机副本、正式切换、24–48 小时观察。P2 不能标为全部完成；当前主机自动恢复仅管理隔离项目。
- 用户说明 Windows 没有现成远程执行入口，已有 ZeroTier 并与另一台 Mac 组网。推荐沿用 ZeroTier + Windows OpenSSH 公钥认证、精确来源防火墙和主机指纹验证；首次须 Windows 本地配合。步骤见 [远程协作交接](../windows-mac-remote-access.md)。远程入口尚未开通；用户已明确选择先完成 Mac 开发，远程接入随 Windows 交接配置。未安装或改动 ZeroTier/SSH。

## 资源与已知限制

2026-09-08 03:38 的 30 秒采样覆盖请求与空闲：OpenClaw 峰值约 1.29 GiB、末次约 1.05 GiB；账本约 48 MiB；CPU 采样峰值约 52%/2.2%。这不是全天峰值或最终全业务压测。之后界面立即空闲约 71 GiB，随镜像构建和备份变化。

早期 Settings 的可用 95.48 GB 包含可回收容量，单用文件系统立即空闲约 20.98 GB 判不足不完整，已纠正。没有清理个人文件、快照或废纸篓。

Docker.raw 的实测逻辑大小约 228.3 GiB，当时实际分配约 2.55 GiB。曾配置的 40 GiB 预算没有缩小已有虚拟磁盘，不能称硬上限已生效；不要通过删除磁盘或卷处理。

Terminal 的 Computer Use 访问曾被工具限制拒绝，未绕过。需要重新登录时使用用户可见终端/浏览器，不输出授权链接、代码或凭据；登录期间不重启其共享 namespace。

## 本机发布证据

- 当前主机服务源提交：`94785405782939c1f32c1f647f8839450ee2195d`，此前恢复实测使用 `fcbfa4a`。只读副本：`~/Library/Application Support/Clawbot/host-releases/<commit>/`，清单 `release-runtime.json` 保存镜像身份和文件 SHA-256。更新/回退实测保留同一测试镜像，业务仍在隔离测试卷。
- 本次实际自动恢复证据：`CLAWBOT_INSTALLED_HOST_AUTO_RECOVERY_OK`，`dataUnchanged=true`，`recoveredWithinSeconds=22`。可重放脚本 `scripts/mac/verify-host-service.mjs` 不调用启动命令，以免手动恢复掩盖后台服务缺陷。
- 第二次加密备份：`clawbot-test-1788816518487-5d411dfecf`；两个备份均含通过恢复检查后才生成的验证标记，状态页已实际显示。源数据与密钥始终未进入 Git。
- `production-releases/` 中的产物仅本机待用，未激活。已完成源码身份加固的待用产物来自 `88755d6ca51183dd635573d8a6124841a8d6966d`，两个镜像内只读标记均实测匹配，缺少切换门禁仍拒绝启动；旧预备产物不作为最终切换依据。启用前必须再核验。
- 此段发布时立即空闲约 67 GiB，后续数值以状态页实时读取为准。Docker 虚拟磁盘硬上限仍未实测生效。后续真实有限文件系统 ENOSPC/SQLITE_FULL 演练已通过，不能据此声称物理磁盘损坏也已实测。

### 本次末尾补充验收

- 生产 runtime 内容身份：`sha256:a4d6f326f75551f7d434f57c9bb19be175a6fd19fd7f1d2cf4caa69fb765fe5e`；guard：`sha256:6a7c1ff5e505503b80db93ccd753e58960e13969b07052536f1c08a6e3226b3a`。位于本机 Docker，不曾推送镜像仓库或启动正式服务。
- `verify-test-network.mjs` 实际暂时断开唯一测试 origin 的 Docker bridge 接口：ChatGPT 公共站点的无授权连接失败，共享回环账本保持可达；重接后出站连接恢复、服务身份/健康复验通过。未修改宿主 Wi-Fi，未中断 Windows。此证据不覆盖真实微信轮询、离线消息补收或生产 Tunnel 重连。
- 本段网络检查时宿主使用 `fcbfa4a`；后续已受控更新至 `9478540`，详情见完整清单。仓库新增提交不自动改变已运行的只读副本，镜像更新仍须独立验证。

### 当前接续入口

P1 原型证据保留如上；后续已经完成三服务生产运行器的合成故障矩阵、规范化包/九卷导入、故障核对与同版本恢复选择。当前完整状态及未完成项统一见 [Mac 当前清单](../mac-before-windows-checklist.md)。生产版本更新/失败回退的共用逻辑及合成演练现已通过；候选固化、持续 24–48 小时与最终资料审计仍需接续；Windows 最终数据、正式切换和真实验收留到远程交接。旧段中的待用镜像和空间数字为当时快照。
