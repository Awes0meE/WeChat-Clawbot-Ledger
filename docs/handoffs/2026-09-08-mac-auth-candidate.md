# Mac 授权恢复候选镜像检查点

2026-09-08。候选源码为 `897f2ab296b5947baf00471f3a081c16d6689766`，已推送 `feat/mac-docker-runtime`。本次构建、归档和未启用检查完成；在线测试宿主仍为 `0d07f3b`，Windows 仍负责生产。

## 固定产物

| 产物 | 身份 |
| --- | --- |
| Runtime ARM64 | `sha256:c903cd0e332cc2b5bdae19e1d9bf79c3f1d265c431e5b857f12ebf6c8f3842f5` |
| Guard ARM64 | `sha256:2e31c6a216a20b375328e78d2daeae5d49d2a7429cd89473244f148e20ad02b3` |
| ezBookkeeping | `mayswind/ezbookkeeping@sha256:1043c95201f0432cd30328f6feb9a5b57359449400ea54fdf9a0c6138ab4c227` |

私有产物位于 `~/Library/Application Support/Clawbot/`：`production-releases/<完整源码提交>/` 保存清单和 Compose，`image-archives/897f2ab296b5947baf00471f3a081c16d6689766-a2e7c77d-2679-4161-916e-a2d0a1c3ced7/` 保存镜像、对应源码 bundle 和核验记录。按镜像内容 ID 导出，不携带仓库标签、业务卷或凭据；重新加载、源码 bundle 验证及容器清单不变检查通过。旧恢复资料保留。

## 打包修复与验证

原 `.dockerignore` 的 `**/*token*` 同时排除了五份授权源码，导致新 entrypoint 无法加载令牌更新模块。本次仅豁免五个明确的源码路径。真实 BuildKit 合成上下文验证它们存在，模拟 token 数据、环境文件、SQLite 和测试账户文件仍被排除；没有读取真实测试账户文件。

发布入口新增镜像内容核验：runtime 的 151 份及 guard 的 10 份源码必须与当前提交的 SHA-256 相同，且为只读普通文件，验证完成后才发布清单。缺少新模块的旧候选被实际拒绝；新候选全部通过。

- 可移植回归共 607 项：606 通过、0 失败、1 跳过；58 个测试文件执行，11 个 Windows 操作文件另行验证。
- 微信插件构建和 22 项测试通过，构建没有产生额外 dist 差异；不将构建等同于完整 TypeScript 类型检查。
- 未挂源码或业务卷、断网运行新镜像：实际入口到达生产启用检查，并因缺少策略文件拒绝启动，无模块缺失错误。授权恢复模块加载、受限 MCP manifest 及固定 cloudflared 二进制哈希通过。
- 实际宿主 CLI 在未启用状态保持 disabled，未确认的启用回执被拒绝。没有创建生产容器、安装生产任务或生成启用门禁。

上述证明打包内容、模块加载和拒绝路径；完整生产宿主正向操作、具体版本业务兼容、真实微信和公网验收仍未完成。此前源码挂载的合成演练不能替代新镜像完整运行验收。

## 登录与连续观察

用户完成本机 ChatGPT 登录后，2026-09-08 11:25:11 UTC 再次核验：独立授权暂存报告 credentials-present，无告警；固定 `gpt-5.6-sol` / 官方 `codex` 返回准确测试短句，工具数为 0。私有授权及模型核验回执保留，用于后续导入；尚未导入在线测试或生产，未发送微信、写账或切换 Windows。

在线宿主 `0d07f3b` 在 11:26:56 UTC 的原始观察快照：142 个样本、0 次断采、6 个非健康样本，最长连续健康约 79.2 分钟，当前连续约 1 分钟；24/48 小时均未达标。运维期间的非健康记录保留，不拼接旧宿主时长。

本次临时核验容器和已完成测试日志已清理；真实登录暂存卷、验收回执和恢复包继续保留。下一步完善授权状态汇总及候选整体运行验收，继续真实稳定观察，再形成 Mac 独立准备结论。
