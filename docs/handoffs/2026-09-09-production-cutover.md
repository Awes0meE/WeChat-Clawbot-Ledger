# Windows 到 Mac 正式切换

2026-09-09（Asia/Singapore）。Mac 正式服务及简洁生产看板当前均为 `8a9b796`。真实历史查询、直接记账、确认后入账、待确认后取消及公网验收通过；最终九卷备份、Windows 副本与第二处密钥的独立恢复、两端测试退役完成。Windows 四项任务已停止并禁用。P1–P6 迁移执行与验收、文档收尾和独立公开镜像同步均已完成。下文按操作顺序保留历史状态，以本段及文末为当前结论。

## 最终停服与数据

- 按已核验任务、进程创建时间、程序和参数停止三项生产服务；禁用 Gateway、Tunnel 和账本任务自启动。原任务定义及进程证据留在 Git 外私有目录。Windows 测试账本暂时保留。
- 最终导出后再次核验三项任务禁用、正式端口关闭。原始备份保留所有普通文件、WAL、授权、任务、配置及三个 Windows junction 的目标记录。
- 原始归档 576032768 字节，SHA-256 `cac4be365a5f55937fbcb72223b4d3b39f9075e22ef1a56eb02e25409f19c02f`。迁移归档 142494720 字节，SHA-256 `673e72d00e2fe63fc58b5ef5b9596f5c7b26362b6d63a4e4410667ad0faa89b3`。两份经固定主机指纹的 SSH 传输，在 Mac 重新计算哈希一致。
- 九卷清单共 9250 项，13 个 SQLite 转成包含已提交 WAL 的一致 DELETE 快照。账本全 16 张表、回执全 12 张表的 schema/全部行审计与原始字节副本一致。原始包不做原位数据库打开或规范化。
- 规范化保留 Windows 原安装文件于原始包，导入排除已识别的平台程序目录。两组已核验的空 Gateway 锁数据库及 512 字节 journal 只从导入副本排除，原始包保留。WAL 文件枚举失败、Windows 规范路径修正和空锁文件拒绝均在副本处理，没有覆盖源数据或放宽导入策略。
- Mac 解包只接受普通文件和目录，拒绝链接、重复路径、路径穿越和超预算归档；清单 SHA-256 为 `792a0dbb26d5b5b586e985d84c7ad3d77d14f10371f0da7c7ca8df4bfee0e39f`。
- 使用固定 `9f3c0e5295fe62185c7fc7863e1315745390839a` 同提交干净检出调用正式导入入口，创建九个新生产卷并验证全部 9250 项、权限及两库 16/12 张表。结果 `CLAWBOT_MIGRATION_NEW_VOLUMES_VERIFIED`，activated=false。未重建或替换通过验收的镜像。

## 当时的后续顺序（导入后、启用前）

核验实际官方模型调用、唯一 owner/接收游标和 Windows 持续停服，随后启用 Mac。正式微信与公网验收、生产看板、加密备份和异机恢复校验完成前，不宣称迁移完成。Windows 保留最终快照和恢复资料，不自动回滚数据库。

## 启用前授权与身份核验

- 不启动接收器，从已导入的正式 codex-state 复制原始授权到独立新校验卷；固定官方 Codex harness 和 gpt-5.6-sol 实际云端调用返回预期文本，零工具、零业务写入。校验后授权字节与正式卷一致，没有触发授权替换。
- 正式卷中的唯一账号、owner 配对、接收游标及运行配置与已验证最终包完全一致。三个生产容器已创建但未启动；真实宿主及生产看板不可变包已准备，启用审核模板尚未确认。
- 本次模型成功只能证明当前官方模型授权；真实微信、公网及新正式写入仍须启动后单独验收。

## 首次启动目录修正

首次账本启动拒绝缺失的 `ledger-data/log` 目录。核对正式卷及切换标签后，由 uid/gid 1000 创建该空目录（0700）；未改数据库、配置或镜像。该日志目录是 Windows 到 Linux 路径转换所需的运行目录，后续导出规范须显式准备。守护按原规则重试，未绕过健康检查。

## 首次接收器启动路径修正

官方启动迁移发现内部 `agent_databases.path` 仍为 Windows 反斜线相对路径，移除两个无法解析的索引后拒绝就绪。首次启用因此未完成，门禁移除；随后核实三服务全部停止、宿主 disabled，无微信接收或公网发布。已核对并卸载本次失败的确切宿主任务，原 plist 保留。

只从最终快照取回 bookkeeper/main 两条完整索引行，把 `path` 的分隔符转换为 Linux `/`，确认对应原数据库文件存在；其他字段不变，主状态库 integrity_check 通过。没有重建 agent 数据库、删除会话、清空去重库或运行不受限的全局修复。原始导入包及 Windows 快照不变。日志目录与 registry 路径均修复后重新执行正式启用检查。

## 正式启用、公网与生产看板

三项 Mac 正式服务就绪，正式启用入口返回 `CLAWBOT_PRODUCTION_HOST_ACTIVE_REQUIRES_WECHAT_AND_PUBLIC_ACCEPTANCE`。官方状态迁移完成后按其提示重启接收器；在重新核验账本/接收器就绪、门禁和所有输入之后启动公网守护，三服务身份及 namespace 验证通过。

公网现有验收入口通过，包括账本页面、响应策略，以及携带真实 HTTP/MCP 授权的公网访问拒绝。未修改 DNS、Tunnel 身份或主站路由。生产看板切换入口通过，端口 18990 返回 profile=production；切换时业务容器及启动时间保持不变。当前模型卡只表示凭据可读，不替代先前实际推理验证。

首次真实微信历史查询返回通用错误，恰逢接收器启动恢复，正在跟踪重试；不能宣称微信业务验收通过。生产看板微信状态返回 response-unrecognized，Tunnel 摘要曾显示 stale，诊断展示仍需核对真实运行响应。正式备份、异机副本和旧测试实例退役尚未完成。

## 正式微信故障与 Codex 跨系统修复

两次真实历史查询均正常到达微信插件，失败在官方 Codex 初始化 native SQLite，尚未进入账本查询。Windows/Linux Codex 0.151.0 的六库迁移版本一致；实际源库全部 64 条校验值逐项匹配固定版本官方 SQL 的 CRLF 形式，新建 Linux 参考库全部匹配 LF 形式。没有升级 harness、改二进制或取消校验。

先进入正式维护并完成九卷 AES-GCM 备份及独立恢复校验：11034 项、账本 16 表、回执 12 表通过。原六库离线保留，校验值修复仅在新副本执行；全部 schema 和除 checksum 外的数据逐表审计一致。

修复副本能正常调用官方模型；进一步验证发现摘要读取成功并不代表历史可读，官方完整 thread/read 报 missing source rollout。对实际 68 个原线程严格映射 rollout_path 与 cwd，检查全部对应普通历史文件存在，其他数据与对话文件保留。修复后官方完整历史读取成功，实际模型调用再次通过，零工具、零业务写入。

相关可复用代码分步提交于 `589c1a4`、`9d4c904`；五项 SQLite/WAL/路径测试分别在 Mac 和 Windows 通过，没有平台跳过。支持 LF 与已核验的 Windows CRLF 两种目标，不移除 Windows 接口。入口与来源要求见 [跨系统兼容说明](../codex-state-cross-platform.md)。

在正式维护及操作锁下，核对源、备份和副本证据后，于 2026-09-08 18:06:55 UTC 安装六库（64 条 checksum、68 个线程路径）。原六库及 sidecar 在私有卷中另行保留；安装前后账本和回执未被修改，正式配置和镜像仍为 9f3c0e5。正在按原守护顺序恢复服务，真实微信重验仍待完成。

## 修复后的真实查询与连接问题

用户随后确认原微信会话的最近十笔历史回复正确，无需 `/new`。实际工具绑定及权威回复发送日志与这次请求吻合，真实历史查询验收通过；不能据此替代新记账及确认／取消验收。

用户还报告回复后短暂出现“无法连接到 OpenClaw”。同期捕获宿主停止并重建接收器。只读 readiness 采样确认：启动后 124 秒返回 HTTP 503，failing 仅为 `openclaw-weixin`，事件循环正常；授权诊断为 `response-unrecognized`。模型故障已解决，连接健康问题仍须修复。已在本次回复结束后进入维护，避免继续循环重启。

状态页 Tunnel 证据的有效期为十秒，旧后台缓存却保留五分钟，导致过期误报。`8b7f845` 改为独立短周期刷新，模型等凭据检查仍保留原间隔，十二项定向测试通过。新不可变看板已准备，等待业务恢复后安装。

维护期间确认三个生产服务均停止后，使用原客户端与只读正式状态做一次轮询，仅输出字段形状，不启动另一接收器、不保存返回游标、不处理消息。实际服务器响应包含 `msgs`、非空字符串 `get_updates_buf`、`sync_buf`，省略 `ret` 和 `errcode`，并非本地超时。原诊断把省略零值状态码的合法响应判为断线，导致 readiness 失败。

适配器现在接受这种完整轮询结构；仍拒绝空对象、错误字段类型、不完整结构、显式错误及本地超时。Mac 构建及插件全 25 项测试通过。修复同时保留 Windows 原部署路径；待发布后验证实际轮询与超过原两分钟窗口的 readiness。

## 轮询修复正式发布

源码修复 `f391f4b`、已编译运行文件 `62cfd9e` 分步提交并推送。Mac 插件全 25 项、Windows 对应 5 项测试均通过。新 ARM64 runtime/guard 的 162 项镜像源码检查通过，官方 native Codex 二进制与旧版 SHA-256 相同。

更新前正式九卷备份及独立恢复通过，共 11102 项、账本 16 表、回执 12 表。再用该当前备份创建独立保留候选和九个新生产运行卷，只变更两处源码绑定元数据及批次凭证；全文件与两库审计通过。正式 release-update CLI 完成新旧宿主切换并确认新宿主仍在维护，保留旧九卷、旧镜像和原配置。随后明确 resume 恢复服务。

生产看板也更新至 `62cfd9e` 并绑定新宿主，使用已核验的确切任务与进程操作，更新前后业务门禁、宿主 PID、三服务身份／启动时间／运行状态完全一致，原任务及私有 journal 保留。当前本地三服务健康，实际微信轮询状态为 last-poll-accepted，公网验收再次返回 LEDGER_PUBLIC_ACCEPTANCE_OK，Tunnel 展示持续为有效采样。

新版三镜像及 Git 源码恢复包已导出、重载复核，并复制到 Windows 私有目录；四项文件哈希全匹配。此处是程序恢复资料，不是最终生产数据异机恢复验收。真实新写入／确认／取消、最终数据备份及第二处密钥与测试退役仍待完成。

修复后已固定 76 条采样、379.29 秒连续健康记录；接收器身份和启动时间不变，没有再次重启，微信实际轮询被接受，Tunnel 无过期样本。这覆盖此前约 124 秒发生的健康误判窗口，不额外重启已完成的三小时准备观察。

两个本轮已完成实际模型验证的授权暂存卷已核对专用标签、初始化标记和零容器引用后删除，验证记录保留；生产接收器身份、启动时间和健康不变。生产备份密钥已通过 SSH 复制到 Windows 的独立私有密钥目录，逐字节哈希、长度和仅当前账户可访问的 ACL 验证通过。此处不输出密钥或其哈希，密钥不与镜像／数据归档放在同一目录。最终生产数据异机恢复仍待新写入验收后执行。

为准备退役，旧 Mac 测试环境已重新核验两个服务与七卷的隔离边界，并完成最新 AES-GCM 七卷备份、独立新卷恢复及全文件／两库验证。临时恢复卷已由备份工具清理，原测试服务按原流程恢复；这一步尚未删除原测试实例，也没有操作正式数据。当前等待用户按已给出的微信步骤完成待确认后取消，再验证确认入账。

轮询测试结束后，核对 Windows 专属测试目录恰好为三份已知文件、哈希一致且无链接，再删除该目录；同时按六文件白名单清理本机用完的一次性探针／传输辅助脚本。正式服务、原始快照、源码镜像归档、密钥及验收回执均保留。

## 异机恢复与真实取消验收

新版当前生产九卷再次进入维护制作一致备份，独立恢复通过，共 11111 项、账本 16 表、回执 12 表，随后恢复正式服务。把加密数据包及两个清单传到 Windows 私有目录，核验三份文件哈希和仅当前账户可访问的 ACL，再从 Windows 下载数据包及独立目录中的第二份密钥。在不挂载生产卷、网络关闭的九个新临时卷上完成解密和全文件／两库恢复比对，结果为 CLAWBOT_HISTORICAL_BACKUP_RECOVERY_VERIFIED；临时恢复卷自动清理。这证明异机保管的真实文件可恢复，未把本机原副本代替下载文件做测试。

用户随后发送三条新微信消息并认为回复正常。逐轮核对：占位文字得到格式说明且未调用记账工具；具体疑问消费调用 prepare_expense；取消调用 resolve_expense_confirmation，工具与最终回复均明确未入账。成功入账回执数量保持不变、有效待确认记录为零、已处理确认标记增加一条；这三轮期间没有服务重启或连接错误。真实待确认／取消验收通过。

已请用户用一笔确实发生且尚未记录的消费完成确认入账，避免用虚构测试金额污染账本。该验收仍待完成；其后的最终备份须覆盖新交易及最新去重／游标，不能沿用本节较早的快照冒充验收后的数据。

## Windows 测试退役与公网归属复核

核对 Windows 测试任务的程序、完整参数、工作目录、回环监听和原进程创建时间后，保留任务 XML 及原安装／数据，禁用并停止该确切任务。再次确认四项 Clawbot 生产／测试任务全部禁用且未运行，8888、18888、18789 均无监听。未卸载 Windows 部署接口，没有执行 Windows 整机重启；已验证的是现有自启动配置被禁用。

Mac 公网验收再次返回 LEDGER_PUBLIC_ACCEPTANCE_OK，正式 guard 报告 Tunnel 已注册且发布进程运行。Windows Tunnel 早已关闭并禁用，所以该域名当前由 Mac 提供服务，Windows 后续断网不承担该入口。此检查没有切断 Windows 网络或改变主站路由。

用户报告新消费成功；实际调用 record_expense，新增一条 created 回执，失败记录未增加，API 确认时间早于最终回复。此处通过的是直接记账，不把它记作尚未实测的确认后入账。微信对删除历史账目的请求明确说明无删除权限；额外金额查询返回相应只读结果。具体账目及原文仅留在私有运行状态，不进入 Git。

## Mac 旧测试实例退役

先认证已有加密备份，检查测试宿主自启动不存在，再按容器身份停止两项旧测试服务。停机审计发现账本全表不变，但测试回执的 ended_trusted_runs 一条记录在备份后更新，因此没有继续删除；在保持测试停机的状态下补做七卷加密备份，独立恢复的全文件／两库审计通过。新备份后再次比对两库完全一致。

随后只删除已经核对标签、挂载和引用关系的两个旧测试容器、七个测试卷和一个空测试网络。正式三服务的容器身份、启动时间与健康状态保持不变。新旧测试备份及密钥、原 Windows 快照、旧生产卷和恢复候选均保留，未执行全局清理。

真实直接记账还通过了只读 API 复核：新增回执对应唯一一条账本交易，账号、金额及备注均吻合。确认后入账仍单列待验收，不能用这次直接记账代替。

## 包含新交易的正式备份与收尾

在真实直接记账成功后，再次进入正式维护，制作九卷一致加密备份并独立恢复比对；共 11113 项、账本 16 表、回执 12 表通过，随后恢复正式服务。将该新数据包复制至 Windows 私有目录，核验三文件哈希及访问权限；从 Windows 下载该包和独立目录中的第二份密钥，在九个新隔离卷完成认证解密、全部文件及两库全表比对。结果 CLAWBOT_HISTORICAL_BACKUP_RECOVERY_VERIFIED，未挂载生产数据、未启用恢复候选。临时恢复卷、下载副本及下载的临时密钥已清理；两台机器上的正式备份、原始快照、恢复镜像和两份正式密钥保留。

已完成的旧测试模型授权暂存卷也按确切标签、初始化标记、成功验证记录和零引用删除，验证回执保留。最终再次确认 Windows 四项任务禁用、8888／18888／18789 无监听；Mac 生产状态页恢复健康。此后若再完成确认后入账验收，须追加备份，不把本次快照当成覆盖未来写入。

## 分类纠正重试故障与正式修复

用户报告疑问消费返回“记账结果无法确认”。该消息于 19:12:26 UTC 到达，接收器此前于 19:11:24 已就绪，因此这次失败不是维护停服造成。第一轮 prepare_expense 使用了不在正式目录中的分类，原执行顺序先消费可信消息绑定再校验分类；模型随后纠正分类时失去原绑定，结果 hook 已保存无法确认的回复。只读核验显示此轮没有新增交易，也没有有效待确认提案；成功回执、失败记录和已处理确认数量均未变化。

`4870258` 将分类校验移到绑定消费之前，并在工具执行前拒绝错误参数。只有参数校验产生、形状和原因完全匹配的未执行 blocked 结果跳过未知结果回执；实际执行异常仍保持保守处理。纠正后仍须通过原可信消息、所有者和运行轮次检查，不放宽身份、重复提交或最终成功回执规则。

四项新回归分别覆盖直接记账／待确认与同实例／跨实例纠正：修复前四项均失败，修复后通过。回执与持久化 106 项分别在 Mac 和 Windows 通过，均无跳过；可移植回归 650 项中 649 通过、0 失败、1 跳过，11 个 Windows 操作文件仍由对应平台单独验证。没有修改 Windows 原生 MCP 清单或官方 harness。

正式 ARM64 runtime／guard 由干净的 4870258 源码构建，152＋10 项源码文件验证通过；官方 native Codex 二进制与旧版实际 SHA-256 相同。先备份当时最新九卷并独立恢复出保留候选，离线生成新运行批次，核对全文件与两库后，仅调整源码绑定及批次凭证。正式更新入口完成 62cfd9e → 4870258，维护状态下核验成功后明确恢复服务。旧运行卷、候选、原始快照、镜像和密钥保留。

新版本三服务恢复健康，公网检查返回 LEDGER_PUBLIC_ACCEPTANCE_OK。此前已成功的直接记账仍为唯一对应交易；没有为验证修复自动写入消费。已请用户重新发送上一轮未入账的疑问消费，再回复确认；等待期间保持生产运行。该次真实确认写入与其后的最终备份仍待完成。

新版本三镜像与 Git 源码恢复包已导出并重新加载验证；五份恢复文件复制到 Windows 私有目录，逐份哈希及仅当前账户访问权限通过。该程序归档不包含凭据，不等同于尚待追加的最终数据备份。

## 简洁生产状态页上线

按用户要求，`146da8d` 将首页整理为整体状态、账本／微信记账／公网访问三张卡片和一行供电、磁盘、Mac 运行时长；模型及各项授权检查默认折叠，授权异常仍在首页提示。`f17dbd4` 进一步区分后台操作与真实维护停服，只有三项服务都运行时才说明后台任务期间服务仍在运行。

四项页面回归通过，实际浏览器完成 1280×800 和 390×844 检查，无横向溢出，详情默认关闭且可展开。最终不可变看板 source=f17dbd4，绑定 source=4870258 业务宿主；替换确切看板任务后，业务门禁、宿主进程、三个容器及其启动时间保持不变。旧任务与私有切换记录保留，结果 productionChanged=false。实际生产页面显示运行正常、账本连接正常、微信接收正常、隧道已连接。

上线后已关闭本次专用验证浏览器，核对确切进程后停止 18991 临时预览并删除其脚本。新版恢复包双机核验通过后，移除干净的临时源码检出；两台机器上的一次性 Windows 测试退役脚本和增量传输包逐份校验哈希后删除。原始快照、完整源码／镜像恢复包、正式备份／密钥及验收证据保留。此次清理没有停止业务服务。

## 自然确认与取消回复修复

用户新一轮已收到待确认回执，随后回复“是的”。19:47:53 UTC 的实际工具调用是 resolve_expense_confirmation，但返回缺少可信消息元数据。旧词表接受“是”，未接受“是的”；接收这条消息时被视为实质新请求，旧提案被作废，跨实例确认绑定随后被拒绝。只读核对显示成功回执仍为 30、失败仍为 2、已处理确认仍为 108，无有效待确认项，没有新增交易。未替用户重放确认或自动补账。

用户要求同时接受多种自然表达。`8a9b796` 补充确认侧的“是的、好的、没问题、行、记入、确认、对的”等，以及取消侧的“取消、不记、不要、撤销、忽略、拉倒”等；精确词表直接生成工具说明，避免模型与执行端各认一套。允许句尾句号、感叹号、逗号和波浪号。整条消息含修改、问号或其他实质内容时仍按新请求处理，不能用确认词前缀触发写入。撤销只取消待确认提案，不删除已入账交易。

原“是的”故障在重启、同实例和跨实例三项回归中修复前均失败。扩展后 37 个定向场景全部通过，涵盖用户列出的说法、标点、原消费时间保留、取消零 API 调用、重复确认不再写入和混合内容不误确认。Mac 可移植回归 686 项中 685 通过、0 失败、1 跳过；Windows 回执／持久化 141 项全部通过，无跳过。此前简洁状态页四项测试也已在 Windows 通过，独立源码恢复包已保留。

通知短时维护后，当前正式九卷备份与独立恢复通过：11113 项、账本 16 表、回执 12 表。使用这份最新备份准备新九卷，完成全文件／两库比对和 4870258 → 8a9b796 正式更新。152＋10 项镜像源码检查通过，官方 Codex 二进制哈希不变；旧运行批次、候选和原快照保留。

显式恢复后，三服务健康，微信最近轮询被接受，公网返回 LEDGER_PUBLIC_ACCEPTANCE_OK。简洁生产看板更新到 8a9b796 并绑定新业务宿主，切换记录确认 productionChanged=false。已请用户重新生成上一笔未入账消费的确认单，再以“是的”或“没问题”完成真实验收。等待期间保持服务在线；正式数据备份须在这次确认写入后追加。最后还需完成全局规则／项目文档核对和私有库、独立公开镜像同步，不提前宣称迁移目标全部完成。

## 最终确认入账与异机备份验收

用户回复成功后，只读核对原会话的两轮实际官方工具调用：2026-09-08 20:06:32 UTC 为 prepare_expense 并给出确认单，20:06:56 UTC 为 resolve_expense_confirmation 并给出成功回执。created 回执由 30 增至 31，已处理确认由 108 增至 109，失败仍为 2，有效待确认项为零。账本内对应唯一交易，金额、描述、原消费时间均与该回执匹配；创建时间早于最终成功回复。该交易约 151 秒后被删除，故随后普通 API 列表不再显示。只读数据库核对保留了创建和后续删除事实，没有恢复或重复写入该笔记录。此证据补齐真实“提案 → 确认 → 入账”验收。

随后通知维护，正式 enter／backup／resume 全部成功。当前 `8a9b796` 批次的九卷加密备份完整恢复比对通过：11112 项、账本 16 表、回执 12 表，包含最新确认与删除状态。把三文件包送至 Windows 私有目录，验证哈希及 owner-only ACL，再下载该副本和另一目录保管的第二份密钥。九个新建无网络临时卷的认证解密、全文件及两库比对返回 CLAWBOT_HISTORICAL_BACKUP_RECOVERY_VERIFIED；未挂载生产源卷、未启用恢复候选。临时恢复卷、三个下载文件及下载密钥已清理，双机正式备份与两份正式密钥继续保留。

备份后 Mac 三服务健康，维护标记已解除，公网验收返回 LEDGER_PUBLIC_ACCEPTANCE_OK，18990 页面仍为生产看板。Windows 的禁用任务及保留安装不再承担生产。没有重复三小时观察，也没有执行 Windows 整机重启；已验证的是其当前自启动配置不会启动旧服务。

## 文档、双库发布与临时资源清理

全局规则与项目文档已核对：通用规则无需调整；Windows 原生部署契约保留，Mac 的迁移、备份、授权及看板入口更新，授权开发时间线移入历史记录。公开中文 README 保留原演示图，并提供 Windows／Mac 分别配置的入口，明确全新 Mac 生产向导和 Intel Mac 尚未验收。

私有开发通过 PR #10 合并至 main，保留每个检查点的提交；公开库仅接收经过审核的文件树，使用既有 GitHub noreply 身份独立提交，没有引入私有提交历史。共享 499 个文件逐字节一致；公开导出 501 个文件的秘密／个人路径扫描零发现，61 份 Markdown 无断链。公开副本的完整可移植回归 685 通过、0 失败、1 跳过；另修复目录含空格时校验脚本的 URL 转文件路径问题，未改变生产运行版本。

完成后移除两份干净临时源码检出、公开副本的验证依赖、扫描副本与一次性扫描工具，以及已用完的确认核验辅助文件；已合并开发分支从本机和私有远端清理。公开仓库检出保留供后续维护。原 Windows 安装／最终快照、全部正式数据卷、恢复候选、源码镜像归档、双机正式备份、密钥和验收回执继续保留。最后核验 Mac 三服务健康、生产看板正常、Windows 四任务禁用且三个业务端口无监听。
