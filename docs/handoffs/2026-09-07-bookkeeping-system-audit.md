# 记账与历史查询交叉故障检查（2026-09-07）

用户在历史查询恢复后报告记账再次失败，并要求检查整个系统。修复已提交并发布为 `c05813e16d5c87096dc379fc51c00fad648b0b94`；生产切换、严格本机检查和用户授权的真实微信交叉验收均通过。测试记录已精确清理，账户、分类和交易均恢复测试前基线。

## 故障证据

- 00:55 历史查询的实际工具成功，用户确认收到明细。
- 00:59、01:01 两次新消息均调用了 `record_expense`，均在可信消息绑定检查处失败。工具已可见；失败发生在 HTTP 写入前。
- 同期日志显示 `llm_input` 已关联当前消息，但 `before_tool_call` 没有找到该关联，执行实例也没有可领取的绑定。未输出消息正文、身份或账目。
- 三个独立插件实例共享同一临时 SQLite 的回归复现了相同错误。已安装 OpenClaw 的 hook runner 会按当前 registry 分派；仅依赖一个实例中的 Map 不能覆盖该生命周期。
- Gateway RPC、正式账本健康、两个账本与 Gateway 的回环监听正常，三个正式后台任务状态正常。工作区移动没有改变正式插件与 workspace 的 immutable release 路径。

## 检查范围和已确认问题

检查覆盖消息接收与授权、可信关联、执行与回执、确认生命周期、金额及日期、去重和不确定写入、HTTP 查询与 MCP、启动/迁移/回滚、发布与验证脚本、文档和测试隔离。

| 问题 | 影响与修复 |
| --- | --- |
| LLM hook 与工具 hook 之间只有内存关联 | 持久化按 run 区分的消息关联，在工具阶段重新核验 owner 和完整 conversation；覆盖两种入口与三个实例。 |
| 已结束运行仍可能留下跨实例授权 | 增加共享撤销状态，覆盖缓存、已排队 bridge 和 POST 前检查；不能复活旧运行。 |
| Codex 路径未使旧待确认失效 | 在可信的新非确认消息到达时失效旧候选；重复投递不能误取消当前候选。 |
| 旧确认消息重投会处理新的候选 | 确认消息哈希持久去重，标记与取单同事务；覆盖全部结果与并发。单次事务迁移旧库已领取的消息哈希，升级后新确认不受影响。 |
| 时间文本中的数字被当成多个金额 | 金额识别排除完整日期和时钟片段，复用时间语法；真实多金额、未来日期和非 SGD 仍拒绝。 |
| 本地 slash 命令先执行再授权，空白名单可放行 | 在命令和媒体副作用前完成授权；缺失配对/owner 状态时拒绝。 |
| MCP 开通与生产迁移的新任务立即退出后回滚中断 | 精确任务为 Ready 且端口为空时继续恢复旧配置；未知或脱离任务的监听仍拒绝。 |
| 发布器运行命令的目标与启动任务保护不完整 | Gateway、插件、模型、通道检查与回滚均使用同一个显式配置目标；重启与回滚显式保留原服务定义。 |
| 公网 MCP 的明确 IP 拒绝被检查器误报 | 仅接受 HTTP 400 且 `success=false`、`errorCode=200020`、`path=/mcp` 的严格组合；保持无缓存与凭据保护。 |
| 普通测试读取本机真实登录状态 | 改成合成 fixtures；真实 owner 检查移到必须显式指定目录的只读脚本。 |
| MCP 凭据读取失败透传文件系统异常 | 返回固定错误，避免底层路径及诊断进入 MCP 错误结果。 |
| README 重构后测试仍要求旧的内嵌命令 | 测试跟随 README 到 runbook 的真实入口，并继续验证必要发布参数。 |
| 文档中的敏感文件初筛误拒绝合法源码 | 将 stable-ID 的 `state-dir`、`sync-buf` 两个源码模块及编译产物列为精确路径例外；仍扫描其全部正文，其他 `storage` 路径继续拒绝。 |

## 验证进度

- 每个行为修复先用合成输入复现失败，再运行相应回归；测试不访问正式交易。
- 日期/金额完整入口回归 98/98；公网检查及文档入口回归 135/135；owner 与 MCP 凭据检查 15/15 通过。
- 两个回滚脚本的完整测试 59/59、发布器完整回归 58/58 通过；最后增加的服务定义保护在切换及两条回滚路径先红后绿，并纳入最终全套。
- 可信关联、生命周期及查询的交叉回归 198/198 通过；stable-ID 构建及接收/授权测试 16/16 通过。
- 追加的确认消息防重与历史迁移回归先复现失败，再通过 122/122 两文件交叉回归；覆盖真实双子进程竞争与事务整体回滚。
- 原生 Windows ACL 两项在受限沙箱失败，在必要权限的隔离临时目录中复验 2/2 通过；没有修改 ACL 策略来迁就测试环境。
- 隔离 `18888` 的真实 API 增查改删返回 `LEDGER_CRUD_ACCEPTANCE_OK`，自身测试记录已清理，账户/分类/交易基线恢复。
- 正式 owner 只读检查返回 `OPENCLAW_OWNER_CHECK_OK`。普通测试不再依赖或读取此私有状态。
- 正式 API 的独立令牌认证、可见配置账户唯一性、SGD 币种与全部 45 个分类对均通过只读核验；没有执行正式交易写入。
- 两个插件按实际发布安装范围运行依赖审计，已知漏洞均为 0，未修改锁文件或升级依赖。15 份 PowerShell 脚本全部解析通过。
- 脱敏初筛扫描 211 份 tracked 文件及本次明确列出的 5 份新文件，共 216 份；15 处命中经独立只读复核均为固定合成测试输入，新文件没有额外命中，匹配正文未输出。
- 原正式 release 的完整文件哈希、路径与 ACL 校验返回 `OPENCLAW_RELEASE_VERIFIED`。
- 最终全套 `npm test`：752/752 通过，失败、跳过、取消均为 0，退出码 0，耗时约 263 秒。所有最终源码修复均已包含。
- 新 release 构建、完整哈希与 ACL 校验及生产切换成功；严格本机检查 14/14 通过。实际插件/workspace 均指向新 release，六项工具 allowlist 与原 Gateway 任务定义保持一致。
- 新 SQLite 三表已实际建立，一次性历史哈希迁移已应用；待确认状态按预期失效。
- 验收后文档与工具策略关联检查 146/146 通过。最终 tracked-only 初筛仍为 216 份文件、15 处已核验的固定合成输入，无新增凭据或身份命中。
- 真实微信验收覆盖六项工具对应的完整流程，结果见下节。

## 发布与真实验收

正式发布必须来自已提交的干净源码，创建新的 immutable release，再切换并核验实际加载路径。不得原地改旧 release、恢复账本数据库、补账或重放失败的业务消息。

`ledger-runtime-common.ps1` 已同步到已识别、受保护的 Tunnel runtime，旧 helper 备份在仓库外的受保护备份目录，目标 ACL 完整保留。已安装 helper 与源码逐字节一致，严格本机 runtime integrity 检查通过；未重启 Tunnel 或修改 DNS/规则。旧 release 保留用于回滚。

已只读确认原 `gateway.vbs` 启动任务唯一且对应唯一回环 Gateway 进程。官方 restart 具有在特定漂移时重建服务定义的能力，因此发布器使用 `--preserve-definition`；尚无证据确定先前重启超时的原因，该参数不能被称为超时修复。

用户明确授权 computer-use 在已经选中的记账助手会话发送标记测试消息。02:06–02:14 的实际验收：

| 流程 | 实际结果 |
| --- | --- |
| 明确支出 → 历史查询 → 第二笔明确支出 | 两笔各新增一次，回执金额独立正确；历史查询包含第一笔，第二笔指定的凌晨消费时间正确。 |
| 有疑问的候选 → 备注汇总 → 单独确认 | 候选不写入；汇总仅统计两笔实际记录；新查询使候选失效，之后确认明确回复未记账，交易数不变。 |
| 新候选 → 单独确认 | 确认前不写入，确认后新增且仅新增一笔，回执保留候选消费时间。 |
| 精确金额查询 | 返回第二笔，金额、分类与消费时间正确；没有新增交易。 |
| 精确清理 | 用测试前 ID 集合、完整测试备注、金额、账户、类型与时间核对三笔目标，逐笔 API 删除；账户、分类及全部交易的规范化结果与测试前完全一致。 |

截图与原始聊天未保存到仓库。公网登录页、保护头、HSTS、无缓存、HTTP 跳转与注册关闭检查通过；作品集两个入口的 schema-v2 基线 entries 完全一致。本轮没有重新发送公网凭据，不能称为完整公网凭据矩阵复验。

同一平台 message ID 的真实重投仍缺少触发入口；合成去重回归不能替代。此次未进行 Windows 整机重启，也未重复向公网发送凭据。单次检查不能证明不存在所有潜在缺陷。
