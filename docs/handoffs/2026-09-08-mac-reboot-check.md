# Mac 整机重启验收接续

用户在场授权的整机重启已于 2026-09-08 完成，登录后自动恢复验收通过。Windows 保持在线；本次仅验收 Mac 的隔离测试服务。

## 重启前

已确认插电不休眠、显示器不休眠、网络保持配置。FileVault 当前关闭，macOS 未配置自动登录。Docker 与测试守护均依赖用户登录，尚不具备无人登录自动运行的验收证据。

`node scripts/mac/verify-host-reboot.mjs prepare` 先核验现存测试容器健康和身份，保存并立即复核隔离两库全表内容哈希，然后把本次系统 boot UUID、宿主和镜像身份、快照文件哈希保存到仓库外私有 operations 目录。准备完成后必须没有遗留操作锁或维护标记，再保存提交并推送远端。

当前运行宿主为 `94785405782939c1f32c1f647f8839450ee2195d`，测试镜像为 `sha256:1d3fa022e1259ecee4a5bde1a5763ac5b7b32b7dc85373154b704ea0d10beebd`。当前源码与预构建的未启用生产镜像可不同于运行中的测试发布。

此前准备中已另做停服自动恢复演练；重复演练触发五分钟恢复冷却，须等自行恢复后再保存最终基线。最终 prepare 不额外停服，由整机重启提供本次故障条件。已创建本任务的五分钟恢复检查；同一 boot 时保持安静，登录后才核验，完成后暂停。

## 用户登录后接续

登录 macOS 并重新打开 Codex。本任务继续时，先等待 Docker 和登录守护自行恢复；不要手动启动容器或重建服务，以免掩盖自动恢复失败。使用准备阶段输出的回执文件：

```text
node scripts/mac/verify-host-reboot.mjs check "<私有重启验收回执JSON>"
```

检查要求实际 boot UUID 已改变、宿主新鲜健康且发布/镜像一致、现存容器隔离身份合格、原数据库基线未被覆盖、两库全部表仍一致、状态页 HTTP 200。校验 helper 使用 no-deps，不代替守护启动业务容器。成功仅在仓库外创建独占 verified 回执；同一 boot 执行检查必须拒绝。

通过后记录实际时间和结论，另做提交留痕。若失败，保留原基线及卷，先读取状态定位；不能重存基线后称恢复成功。重启会打断连续健康观察，24–48 小时应重新按采样计算。

## 其他待办

本机代码/镜像归档已实际导出并加载核验，详见维护文档。最新 Mac 可移植回归 523 项，522 通过、0 失败、1 跳过；11 个 Windows 操作测试文件仍排除。

整机验收之后继续故障对账/恢复候选提升操作流程、最终接续文档审计及真实连续观察。真实 Windows 最终备份、ZeroTier/SSH、授权/游标适配、单写者停机切换、公网及真实微信验收均留到 Windows 交接。P1–P6 的总目标仍未完成，不得以这次重启或本地合成演练替代生产接管。

最终准备回执已于 `2026-09-08T05:45:59.467Z` 保存：已恢复健康，两个数据库与基线一致，状态页 HTTP 200；同一 boot 的检查实际拒绝通过，未生成重启成功回执。代码/文档提交 `e6fba48` 已推送远端。后续必须以实际新 boot 的 verified 回执补记结果。

## 实际重启结果

系统 boot UUID 已从基线改变为 `BEF5379C-4725-41E5-9927-56FE457C3EC4`。登录任务 PID 735 从原只读发布启动；最初检查因 Docker/守护尚未就绪被拒绝，守护随后自行进入 recovering，再于 05:52:29 UTC 报 healthy。没有手动启动容器、更新宿主或重存基线。05:52:41.931 UTC 的正式 check 通过：两库全部表、基线文件哈希、镜像/容器边界均一致，状态页 HTTP 200。私有 `.verified.json` 已写入。

本次证明人工登录后的恢复，不证明断电后无人登录恢复；也未增加真实微信、模型调用或生产切换验收。原准备时间为 05:45:59 UTC，不能把准备至最终检查之间的总时长当作服务自动恢复耗时。启动等待已反映在采样中，连续观察从实际恢复健康后重新计算。

重启跟进自动任务在验收后已不存在；更新接口也确认任务不存在，因此没有重建重复跟进。后续直接在当前目标中继续。
