# Mac 看板与授权状态候选

2026-09-08，源码 `9f3c0e5295fe62185c7fc7863e1315745390839a`。Windows 仍负责生产；Mac 在线测试宿主 `d53b535` 和两个隔离服务未更换。本记录是候选构建检查点，尚不是 Mac 独立准备完成结论。

| 产物 | 已核验身份 |
| --- | --- |
| Runtime ARM64 | `sha256:9449936c6d3298de346673eb9bb5943b0e53d92f1a0966d659d2c352464cb0ec` |
| Guard ARM64 | `sha256:396d396e7cb449f05e3a99e146d3489301c84b124a85852f4e453a1ec86d6de6` |
| ezBookkeeping | `mayswind/ezbookkeeping@sha256:1043c95201f0432cd30328f6feb9a5b57359449400ea54fdf9a0c6138ab4c227` |
| 私有发布目录 | `~/Library/Application Support/Clawbot/production-releases/9f3c0e5295fe62185c7fc7863e1315745390839a/` |

生产镜像包含授权恢复及微信运行状态检查源码；生产看板和切换/接续脚本运行在宿主，绑定同一提交源码，后续打包时仍独立核对实际生产宿主。生产镜像没有注入 isolated-test 的有效配置。

本次验证：

- 完整可移植回归：65 个文件、640 项，639 通过、0 失败、1 跳过；11 个 Windows 专用文件明确排除。
- 微信插件重新构建及 22 项测试通过，dist 与提交一致。其 TypeScript 配置仍含 noCheck，本次不声称完整类型检查。
- Runtime 152 份及 guard 10 份打包源码均为只读普通文件，SHA-256 与提交一致。
- 实际生产镜像断网、无业务卷和源码挂载运行时，因缺少生产策略文件拒绝，没有模块缺失错误。
- 镜像内授权相关模块可加载；受限 MCP 工具声明存在，没有原生 MCP server 声明。官方 Codex MCP 模块 SHA-256 保持 `c2fc4d3e09c6eeb83319404e170e6453bd613d75580ab5074c53554a58b3fa0f`。
- 看板接续共用流程完成真实临时任务 SIGKILL 后恢复，实际 CLI 拒绝原切换进程仍存活的记录。具体范围见 [生产看板](../mac-production-dashboard.md)。

13:09:50 UTC 新候选的同源码业务验收通过。构建入口 `59d5ffb` 从精确 `9f3c0e5` 的临时 detached worktree 构建现有 isolated-test target；源码检出已清理。验证镜像 `sha256:73b5039eb54cc0fca0f29ae47666ee3ec07d6a73ea5c4e4ef87b7a656902475e` 的 152 份运行源码与生产候选相同；测试 manifest/marker 明确只接受 18888 和合成 owner，生产镜像仍保留只接受生产配置的 manifest。

运行入口为 `scripts/mac/verify-candidate-business.mjs`（检查点 `5ae1328`），参数是私有生产发布目录及用户已经授权的模型暂存回执。它创建随机项目、全新七卷、独立账本账号和两个不同的令牌；不挂载在线测试/生产数据。只读挂载登录暂存，在内存/临时空间读取单一 OpenAI OAuth profile，通过固定官方存储接口写入临时代理状态；原授权数据库和 WAL 哈希不变，原暂存不会被删除。

实际验收覆盖：

1. 固定账本程序的 HTTP、0.01 SGD 消息去重和重新打开 SQLite、MCP 可读历史及十行限制。
2. 插件跨实例写入、重启注册实例后的确认、陌生人写入/查询拒绝。
3. 官方 `gpt-5.6-sol` / `codex` 报告精确六工具，上游 MCP 模块保持原哈希。
4. 明确记账、提出确认、确认、再次提出确认、取消、汇总、精确金额搜索、历史明细共八回合。六工具均有成功执行记录，每个最终回复等于当前消息对应的持久权威回复。
5. 停止并重新创建临时服务后，账本及回执两库的全部 schema/记录哈希一致；再次处理固定消息仍去重。在线原两个测试容器的身份、镜像、启动时间和健康均未改变。

私有结果为 `operations/candidate-checks/68222adf-7b17-489b-a43b-9cbe7073bdb0.json`。临时容器、七卷、网络、源码检出、授权副本及运行目录已清理；验证镜像和脱敏回执保留供复验。运行器另外拒绝在预检未取得项目归属前清理同名资源，参数缺失的拒绝路径已核验。

这些是同源码隔离配置中的完整业务验收，未发送真实微信，也没有运行生产端口、正式宿主或 Tunnel。真实生产 profile 的正向启用、公网和平台验收仍留到交接窗口。新镜像/源码归档已在 14:38 UTC 导出并重新加载，见 [最终恢复资料](2026-09-08-mac-final-artifacts.md)；旧 `897f2ab` 离线包仍保留。

新归档已在 [三小时观察签收](2026-09-08-mac-three-hour-observation.md) 后使用共用运维锁完成。导出入口要求与候选同源的干净 Git 检出；若文档 HEAD 已前移，可在候选提交的临时 detached worktree 中执行，核验后移除该临时检出。产物仅包括镜像与源码，不带账号或数据卷。实际同源临时检出已清理；最后未启用宿主及未确认启用拒绝复验通过。

12:53 UTC 在线观察连续健康约 77.7 分钟，78 个样本、零断采、保留启动阶段一个非健康样本。要求仍是连续约三小时后无待处理异常，不等待 24–48 小时。当前约 20.3 GiB 立即可用空间，尚有余量；未运行会占用共用锁的新归档或正式宿主操作。

本次临时回归日志、看板任务、合成接续记录、HTTP 候选目录和验证容器已清理。真实登录暂存、旧恢复资料和新待用镜像继续保留。
