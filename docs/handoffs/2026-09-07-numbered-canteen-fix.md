# 食堂编号导致明确支出被拒绝（2026-09-07）

## 原因

本机只读核验当晚请求确认：可信消息匹配成功，模型选择 `record_expense`，采用 SGD 和可信消息时间，金额与分类正确，疑问检测为 false。金额证据校验却把 `Canteen` 名称里的编号计为另一个金额，在领取收据和调用账本之前拒绝请求。通用拒绝回复把金额校验失败说成“金额或语气还不够确定”，未说明实际原因。

真实正文、金额、身份、消息 ID、会话、账本内容与凭据未写入仓库。以下示例均为合成测试数据。

## 修改

- `晚餐 8.4 Canteen12鸡饭` 的编号作为消费地点的一部分，不计为支出金额；支持大小写、空格和编号前的 `#`。保留原文偏移和备注。
- 不依赖模型生成的备注删除数字；`NTUC8.25`、小数、算式和显式币种继续作为金额证据。两笔独立消费仍拒绝，真实疑问仍需要确认，只有地点编号而没有金额不能入账。
- 金额证据失败使用 `rejectionReason=amount` 和对应的明确未入账回复。时间失败仍保留单独的说明；日志仅记录固定原因，不记录正文。
- 插件回归覆盖真实写入接口的模拟响应、完整六行回执、发送前权威回复替换，以及同 message ID 只写一次。

## 验证与发布状态

先运行新增用例确认旧实现失败，再修复。完整回归 856/856 通过；全量执行期间进一步补充了两个显式币种边界用例，并对最终代码运行针对性回归 229/229，覆盖新增用例。微信接收插件构建和 16/16 测试通过，15 份 PowerShell 脚本解析和 `git diff --check` 通过。

正式 owner 只读核验通过；Gateway 和微信通道状态命令退出码均为 0，通道 running。受控发布器的 `ReleaseOnly -WhatIf` 通过。220 份 tracked 文件扫描中的 15 处匹配为原有测试内容；本次七个修改或新增文件的增量检查未发现新增匹配。

按项目规定使用 Windows PowerShell 5.1 执行的严格本机检查 14/14 通过，旧 release 的文件完整性与 ACL 正常。首次误在 PowerShell 7 执行时只有 release 检查失败：该宿主不存在校验器使用的静态 `File.GetAccessControl` 接口；改用 5.1 后通过，没有修改 release、权限或校验器来绕过检查。15 份脚本亦已在 5.1 parser 复验通过。

原请求仅在本机内存做只读校验，新代码已接受相同金额且保留相同备注；没有调用账本或修改 SQLite。未补写正式交易，未发送微信测试消息。

用户已明确授权提交并上线。修复提交及正式 release 为 `1ab154fca487154fb07def1be31ca44faaa7c4a7`。切换前正式版本 `3ed3f1bff5a7119d066f75df03d3f59f5fc0b76b` 已验证并保留。正式配置、三个服务任务定义、账本和收据 SQLite 一致快照保存于仓库外受限备份目录；两个数据库快照均通过 `quick_check`。

使用 Windows PowerShell 5.1 执行既有发布器，两次预演通过；发布与切换分别返回 `OPENCLAW_RELEASE_PUBLISHED` 和 `OPENCLAW_RELEASE_SWITCHED`，退出码均为 0。新包包含 38,397 份经过哈希、结构和 ACL 校验的文件。发布包中的两个修改模块与已验证源码哈希相同；直接导入发布包核心模块验证合成复现案例及币种、仅编号边界，通过且没有调用账本。

切换后实际 plugin/workspace 路径均指向新 release，Gateway 恢复回环监听；发布器的 Gateway、微信通道、两个插件、官方 Codex harness 和模型检查通过。严格本机检查 14/14 通过。六工具策略、模型、owner allowlist 和 bookkeeping config 与切换前完全一致；Gateway、账本和 Tunnel 三个任务定义不变。`message_receipts`、`processed_expense_confirmations`、`ended_trusted_runs` 和 `receipt_store_migrations` 的已有记录与一致快照逐行相同。

公网脚本按既有合同使用独立 API token 验证公开 API 仍拒绝认证访问，登录页、HTTPS、安全响应头、注册关闭和 TRACE 拒绝检查通过；作品集基线比对通过，返回 `LEDGER_PUBLIC_ACCEPTANCE_OK`。未重新执行有效 MCP token 的公网连接检查，未执行限速压力验证。初次省略 API token 路径的调用在参数门禁处拒绝；补齐已配置的本机 token 路径后完成正式验收，令牌与响应正文未输出或写入仓库。

本轮没有补账、重放业务消息、发送微信测试消息或创建正式测试交易；也未改动 DNS、Tunnel、账本数据或服务任务定义。代码回归、部署和本机验收不等于新的真实微信端到端验收。
