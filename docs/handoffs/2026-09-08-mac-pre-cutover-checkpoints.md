# Mac 交接前开发的历史检查点

下文保留各次提交时的阶段记录。“正在接续”“尚未”等措辞只适用于该检查点；当前结论统一见 [Mac 工作清单](../mac-before-windows-checklist.md)。本页不是部署指令。

# Windows 交接前的 Mac 完整工作清单

目标来源：2026-09-08 用户要求在 Windows 停服/交接之前，把 Mac 能做的 P1–P6 全部完成。原 `mac-handoff.md` 只定义 P0–P4；这里将交接所需的运维操作包和最终证据审计补充为 P5、P6，不改变原有切换及长期观察要求。

| 阶段 | Mac 可独立完成的工作 | 验收证据 | 必须留到 Windows/切换后的边界 |
| --- | --- | --- | --- |
| P1 | 固定版本、受限六工具、业务/身份/回复链、ARM64 状态持久化 | 实际八回合模型调用、回归、重建/恢复记录 | 真实微信消息收发与平台重放 |
| P2 | 三服务生产运行器、完整身份检查、启动/关闭顺序、namespace 重绑、存储故障保护、只读发布和状态页 | 与生产共用的运行器驱动隔离三服务故障演练、真实有限文件系统写满、无越界操作 | 真实生产配置、完整 Windows 数据及 Tunnel 凭据启用 |
| P3 | 迁移包合同、导入预检/新卷恢复、全文件/全表核验、拒绝覆盖和错误包、切换及反向迁移操作包 | 合成包/本机测试状态的完整演练，错误包拒绝证据 | Windows 实际路径/认证审核、最终停机快照、唯一写入端切换 |
| P4 | 稳定候选版本隔离长跑、资源采样/增长、网络/Docker 恢复、锁屏/供电策略核验、主机重启步骤 | 时间连续的采样和故障演练；明确区分缺失采样与健康 | 实际整机重启/解锁需合适窗口；生产 24–48 小时观察在切换后 |
| P5 | 备份/恢复/升级/回退/维护的可执行操作包、告警状态、保留预算、离机复制准备 | 正反流程演练、失败保留现场、不覆盖未知资源 | 异机目标/ZeroTier/SSH 在 Windows 交接配置 |
| P6 | 新人可接续交接包、版本清单、当前状态、全部需求逐项审计、远端同步 | 可复现指令、完整证据索引、干净工作区与远端提交一致 | 不把前置准备审计写成生产已经接管 |

完成判断以当前运行和文件证据为准。未完成项持续推进；真实数据/身份/时间要求不能由模拟测试代替。窗口内可以做的短期验证不等于已完成 24–48 小时观察。当前 Windows 仍是生产端，Mac 不启第二个生产接收器或 Tunnel。

## 本轮工作

- [x] 三服务控制器及真实 Docker 故障矩阵（生产共用控制器/驱动，18888 实际账本、惰性接收替身与本地发布替身）
- [x] Mac / Docker 存储余量、写入错误与人工解锁保护（真实 1 MiB tmpfs ENOSPC；不是物理磁盘损坏模拟）
- [ ] 生产宿主入口和不可变发布/更新
- [x] 迁移包预检与新卷导入演练
- [ ] 备份/维护/回退操作包及可观察错误
- [ ] 稳定版本持续采样与证据审计
- [ ] 文档去除互相矛盾的历史“当前状态”，保留历史证据的时间范围

## 2026-09-08 三服务演练证据

`scripts/mac/verify-managed-runtime.mjs` 已实际通过：完整运行身份、守护进程崩溃恢复、origin 容器替换后网络/PID namespace 重绑、存储低水位按顺序停服及恢复阈值、真实 ENOSPC 后锁定停写。演练从新的合成账本起步，没有复制任何现存账户、OAuth、微信身份或账本；只清理本次随机标识的新资源。运行器/边界七项单元测试覆盖未知服务、镜像与命令替换、额外环境变量、秘密挂载变可写、外部卷伪装、权限扩大、未就绪不开放入口及网络暂不可用不反复重启账本。

此证据说明共用运行器的本地执行行为，不能替代 Windows 最终数据、真实 WeChat 流程、cloudflared 公网连接或主机物理故障验收。生产宿主安装及持久故障锁解除工具仍在接续。

## 生产宿主入口（已实现，未启用）

`production-host.mjs`、`prepare-production-host.mjs` 和 `managed-host-release.mjs` 已实现：要求同一源码提交的 runtime/guard 镜像、只读且完整的宿主文件、固定 Compose、最终快照/切换 ID、单独启用文件；配置或镜像身份变化时仅关闭已识别入口，不接管未知容器。启用文件不存在/被撤销时不会启动服务，且会关闭属于该发布的已识别服务。维护模式和存储故障锁均不自动解除。

实际 `verify-production-host-inactive.mjs` 已在没有生产容器、没有启用文件的前提下执行真实宿主程序，结果为 disabled，前后生产容器均为零。三服务演练还补充了真实有限文件系统内的 SQLITE_FULL：失败事务回滚后原记录数量不变，integrity_check 通过。它不证明两个独立业务数据库之间存在跨库原子事务；存储错误后的正式恢复仍需核对是否有账本已写而回执未持久化的情况。

发布入口会生成仓库外的只读宿主包与 LaunchAgent 文件，但当前没有创建生产卷、没有启用生产 LaunchAgent，未生成任何 Windows 已停机的真实声明。准备实际宿主包需要最终快照标识，留到交接流程提供，不能现在伪造。

## P3 导入检查点

[离线导入合同](../mac-migration-intake.md) 与实际九卷合成演练已完成：全文件及两库全表一致、九份完成凭证一致、已有数据拒绝覆盖且零写入、篡改包拒绝、演练卷清理。生产宿主增加清单哈希和九份完成凭证检查，防止启动半导入状态。Windows 实际导出映射、授权适配、停机切换及反向迁移操作仍需接续；这不是 P3 正式迁移完成。

此检查点的 Mac 可移植回归为 518 项：517 通过、0 失败、1 跳过，仍明确排除 11 个 Windows 操作测试文件。生产宿主缺少启用门禁的实际检查再次通过；当前只有两个健康的 clawbot-test 服务，没有遗留导入演练卷或生产容器。

## 连续采样与宿主更新准备

状态页增加新鲜守护状态、完整边界概览和按发布保存的每分钟观察记录。采样记录仅含健康状态、供电、空间、内存和发布身份，在仓库外私有目录保存，最多 6000 条，达到预算停止记录而不删除证据。断采超过 90 秒、时间倒退、异常或维护均重算连续健康时长。短时进程 uptime 不再作为连续观察证据。

`publish-host-service.mjs --prepare` 可生成新的只读测试宿主包而不改现有任务；`update-test-host.mjs <已准备提交>` 只更新已识别的同镜像测试宿主，备份旧任务，并在验证失败时恢复旧宿主。此提交先完成算法测试及可移植回归（519 通过、1 跳过）；实际发布、正向更新、回退和观察起始时间在后续实测记录中登记，不能提前记作完成。

### 宿主更新实测

已将实际测试宿主从 `fcbfa4a` 更新至 `9478540`，回退旧版、模拟新版启用后验证失败并自动恢复旧版，最后再次更新至 `9478540`。过程中发现 launchctl 的卸载返回早于任务完全退出，已修复为等待任务消失再启动，随后三条流程全部通过。两个测试容器仍使用同一镜像，未进行数据恢复或切换生产。

新版状态页已在浏览器验证，显示守护检查和连续采样。观察文件自 2026-09-08 04:41:45 UTC 开始保存；最初两次采样记录更新期间非健康，首个恢复健康采样约为 04:44 UTC，不能把更新过程算入连续健康。后续以实际文件和页面计时为准，尚未达到 24/48 小时。当前宿主只读发布来自 `94785405782939c1f32c1f647f8839450ee2195d`，数据/镜像仍是原隔离环境；新版仓库 HEAD 不等于已运行宿主或生产镜像版本。

## 维护和九卷备份检查点

[维护与备份入口](../mac-maintenance-backup.md) 已实现。共用维护逻辑的三服务实际停机/恢复演练通过；九卷加密备份、新卷恢复、全文件权限及两库全表一致的实际合成演练通过，另覆盖异常退出后非空 WAL 中已提交记录的保留。生产 CLI 等待真实宿主/导入环境，未操作生产卷。

独立历史归档恢复也已完成实际合成验证：不挂载原生产卷，仅从归档恢复到新卷；可选择保留已验证候选，候选无生产标签；错误密钥和篡改密文在目标创建前拒绝。尚需接续数据回退/故障锁人工对账操作、候选提升流程、离机复制准备及最终文档审计。

Windows 规范化包的清单生成入口 `scripts/build-migration-package.mjs` 已加入，并用实际合成演练串联清单生成、九卷导入、备份、历史恢复与错误包拒绝。它不猜测 Windows 路径、不代替停服核验、不覆盖原有清单；真实模式只能在 Windows 运行。

首次容器准备入口已加入：`provision-production.mjs` 仅在未启用宿主、导入及镜像/空间检查通过且目标项目不存在时创建停止容器。实际共用驱动已通过首次创建、重复拒绝及首次有序启动，然后重跑完整故障矩阵。

`activate-production.mjs prepare/enable` 已加入：默认全未确认的回执、30 分钟近期核验、完整版本/导入/切换绑定、停止容器/输入/空间检查、精确初次任务安装，以及失败时撤销启用。未确认回执已通过实际 CLI 拒绝测试，未创建生产任务/门禁/容器；真实正向启用留到 Windows 最终交接。异常后人工对账、离机恢复资料和候选提升仍在接续。

复制包工具已通过合成演练：复制前/后完整密文认证、逐文件哈希、仅复制归档及元数据、不带密钥，损坏源在目标创建前拒绝。未向 Windows 或其他远端传输；最终目的地和密钥第二份保管随远程交接配置。

2026-09-08 Mac 整机重启后人工登录、隔离服务自动恢复及两库一致验收通过；实际证据见 [重启验收记录](2026-09-08-mac-reboot-check.md)。Windows 服务仍在线，未执行生产接管。

存储故障的 [只读回执/账本核对入口](../mac-storage-reconciliation.md) 已完成，5 项定向测试通过，覆盖 64 位交易 ID、未知/待定回执、跨所有者和重复交易关联、输入不变及报告拒绝覆盖。只读核对本身不解除故障锁。另行实现的 production-storage-recovery 入口绑定当前九卷备份和人工审核，五项定向测试及三服务维护/恢复演练通过；生产 CLI 尚未实际执行。候选提升和跨版本更新仍在接续。

[独立恢复批次](../mac-recovery-generations.md) 的描述、卷身份、启用绑定和九份完成凭证检查已实现；12 项相关定向测试通过，原九卷/WAL/历史恢复演练通过。候选复制、九份完成凭证、两次恢复批次往返及重复拒绝的合成演练现已通过；同版本宿主选择及失败回退现已实现，并通过实际合成 Docker 与独立 LaunchAgent 生命周期验证。切换成功仍保持维护，旧卷保留；真实生产 CLI 未执行，也未生成或启用真实生产恢复批次。

## 授权、看板与候选业务的逐次检查点（13:09 UTC 以前）

以下从当前清单移入，保留各次提交时的未完成项；现行观察目标为三小时，当前进度见页首链接。

生产版本更新进展：[离线准备入口](../mac-release-updates.md) 已完成并实测不同源码/镜像的新九卷，WAL 数据与非绑定文件全部保留。宿主版本切换、部分创建失败回退和加载失败回退的共用逻辑也已实测；升级后新增合成记录在反向准备中保留。真实生产 CLI 尚未执行。

授权恢复最新检查点：`891d0c8` 已补上保存后的 HTTP/MCP 临时账本副本验收；真实账本程序完成合成令牌接受、撤销拒绝及新令牌恢复，源文件保持不变。维护/备份绑定、保存和中断续写已实现，13 项定向测试再次通过。仍需新镜像固化、真实宿主正向验收、其他授权恢复及长期观察，未更换在线令牌或运行镜像。详情见 [授权恢复](../mac-authorization-recovery.md)。

微信状态检查点 `5893600`：固定插件新增脱敏失效状态，明确 -14 与本地超时/网络失败的区别；超时不再保存游标，授权失败保持到服务器明确成功。插件构建和全部 22 项测试通过，尚未发布运行镜像，未进行真实微信登录或验收。同账户重新扫码恢复、Tunnel 授权处理和看板接入仍待完成。

微信凭据保存检查点 `b90c98a`：已有同账户、同所有者、同 API 地址的单文件更新组件，原游标、配对、其他账户和未知状态保留；本机及 ARM64 三项测试通过。仍需交互扫码暂存及宿主维护/备份入口串联，不代表已经更新微信授权。
## 微信暂存检查点

`ff118f4` 的独立扫码暂存已完成维护/备份绑定入口、六项定向测试与真实 ARM64 官方扫码协议的离线合成演练。与三项保存测试共九项通过；演练无真实扫码、无业务卷写入和持久临时实例。尚未完成暂存导入目标及保存回执间隙恢复，运行镜像未更新，P4 不整体勾选。详见 [授权恢复](../mac-authorization-recovery.md#微信独立扫码暂存)。

后续保存组件已补充写后回执缺失的只读核验；实际子进程在 rename 后 SIGKILL 的故障用例通过，保存测试增至四项，均在 Mac/ARM64 通过。宿主导入和回执续写仍待接入；不自动处理 rename 前的临时文件残留。

`28999f3` 已接通宿主导入和 started/saved 回执续接，16 项定向测试与实际 ARM64 容器组件的断网演练通过，其他八卷审核与原账本令牌审核通过。生产宿主完整正向操作、真实扫码/恢复、候选镜像重新固化仍未完成；不能据内部演练将 P4 标记完成。当前入口见 [微信导入](../mac-authorization-recovery.md#微信暂存导入的宿主维护入口)。

Tunnel 后续检查点：`888c755` 的脱敏诊断和 `8222a42` 的同身份维护凭据替换已实现；10 项保存/回执测试在 Mac 与 ARM64 通过，真实断网容器的保存、重复只读验证、固定配置检查及单文件排除审核通过。运行镜像仍未更新，真实生产入口、Cloudflare 接受和公网恢复未验收。后续应固化候选版本并完成适配/恢复整体回归，继续积累真实稳定观察。

## 先前候选检查点

`897f2ab` 已固化前述授权恢复代码，修复 token 源码被打包规则误排除的问题；161 份镜像源码、实际模块加载、固定二进制及未启用拒绝通过，镜像/源码离线包已重新加载核验。上面的逐项检查点保留当时范围，当前候选身份以 [新记录](../handoffs/2026-09-08-mac-auth-candidate.md) 为准。在线宿主和测试镜像均未改变；真实授权暂存仍保留，未导入服务。

11:26:56 UTC 当前宿主观察为 142 个样本、0 次断采、6 个非健康样本，最长连续健康约 79.2 分钟，24/48 小时未达标。仍需授权状态汇总、候选完整运行验收与最终需求审计，P1–P6 不整体标记完成。

## 观察时长调整（用户决定）

2026-09-08 用户将观察要求改为约 3 小时：连续健康达到 3 小时、没有待处理异常后，结束本轮观察验收并进入后续步骤，不再等 24–48 小时。开发与观察并行。判定仍基于同版本逐分钟实测，断采、维护、异常及时间回退不算健康时长；不以容器 uptime 替代。上文 24/48 小时为调整前的历史口径，现行标准以本节及 `scripts/mac/observation.mjs` 为准。

调整已在 `d53b535` 发布到看板。两项采样测试通过，页面脚本实测覆盖不足/达到 3 小时、过期、未来时间和连接中断；在线 HTTP 状态已确认 `requiredMs=10800000`。发布前后两个业务容器身份、启动时间和重启次数不变。新宿主在 11:34 UTC 开始独立记录，首个启动阶段样本为非健康，连续计时从后续真实健康样本开始；尚未达标。没有新增持久测试实例或临时文件。

账本授权状态汇总检查点 `b46868f`：API/历史查询的独立只读卡片及双容器身份、重启、时效核对已实现，16 项定向测试与实际打包组件检查通过。宿主候选已准备，在线 `d53b535` 继续观察，新卡片尚未上线。11:42 UTC 连续健康约 7.2 分钟，无断采，启动阶段 1 个非健康样本保留。

微信运行状态检查点 `f5f7895`：六项新增测试、十一项相关回归、固定官方命令断网演练及打包后隔离检查通过。新看板候选同时包含模型、账本、微信卡片，尚未安装；在线仍为 `d53b535`。生产看板汇总和完整候选运行验收继续开发，真实微信验收留待交接。

生产汇总检查点 `7062851`：21 项定向测试及真实未启用 CLI 路径通过；检查中维护/宿主或容器切换使结果失效，输出无账户/容器身份。尚未接入生产 HTTP 看板与看板任务切换，完整生产正向验收仍在交接窗口。12:02 UTC 在线观察连续健康约 26.7 分钟，未到 3 小时。

生产 HTTP 看板检查点 `007b662`：页面、后台授权缓存、22 份文件的不可变打包与独立任务模板已完成，12 项定向测试和真实未启用 HTTP/篡改检查通过。正式安装及测试看板任务切换尚未接入；见 [生产看板](../mac-production-dashboard.md)。在线 `d53b535` 未改变，12:18 UTC 连续健康约 43.1 分钟，未到三小时。

生产看板任务切换检查点 `08cf111`：14 项定向测试、真实临时任务成功切换/失败回退/旧任务未退出时恢复，以及未启用生产拒绝均通过。当前线上任务与两个测试容器未更换，临时资源已清理。强制中断后的接续入口、最终候选完整运行验收和交接审计继续推进。12:40 UTC 连续健康约 65.6 分钟、零断采、启动阶段一个非健康样本；三小时尚未达到。

`9f3c0e5` 已完成看板中断接续及新镜像构建，代码回归/打包/未启用拒绝通过；新归档和候选业务运行验收待完成。详情见 [最新候选](../handoffs/2026-09-08-mac-dashboard-candidate.md)。12:53 UTC 连续健康约 77.7 分钟，无断采，三小时尚未达标。

13:09:50 UTC 同源码候选完整业务验收通过，运行器检查点 `5ae1328`，临时容器、七卷、网络、模型授权副本和运行目录已清理。原测试容器与授权暂存不变。剩余 Mac 独立工作为三小时观察结论、新候选归档/宿主拒绝复验和最终接续审计；实际生产配置验收留待 Windows 交接。
